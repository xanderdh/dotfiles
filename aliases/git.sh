#/bin/zsh

# git push upstream master and open the repo in browser
# TODO:
#  add propper function to detect gh
#  add rest of the aliases, and remove oh-my-zsh git aliases
alias gpsupo="gpsup && gh repo view --web" 
